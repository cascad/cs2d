
# Конфиг клиента CS2D.
# ip/port — адрес по умолчанию для строки ручного ввода в меню.
ip = "127.0.0.1"

port = 6000
webtransport_port = 6001
# cert_digest — ОСТАВЛЯТЬ ПУСТЫМ. Digest сертификата меняется при КАЖДОМ
# рестарте сервера, а этот файл ВШИВАЕТСЯ в wasm-сборку: прописанное здесь
# значение блокирует автоматический HTTP-фетч свежего digest и после первого
# же рестарта сервера даёт вечный CERTIFICATE_VERIFY_FAILED у всех игроков.
# Для разового ручного override используйте #hash в URL страницы (не вшивается).
cert_digest = ""

# name/password — аккаунт для авторизации и строки в таблице очков (Tab).
# Первый вход под именем регистрирует его (с паролем), далее пароль сверяется.
# Пустой пароль допустим. Для нескольких клиентов на одной машине задайте РАЗНЫЕ
# name (иначе второй получит «Аккаунт уже в игре»).
# ВАЖНО для wasm: файл ВШИВАЕТСЯ в браузерный клиент при сборке. Ровно "Player"
# (или пусто) ⇒ каждому браузерному игроку генерится СЛУЧАЙНОЕ имя (Player#a1b2).
# Любое другое имя станет ОБЩИМ для всех открывших ссылку — второй и далее
# получат «Аккаунт уже в игре». Локально под себя менять можно (native).
name = "Player"
password = ""

# [[servers]] — список серверов для мини-лобби. Каждый проверяется на
# активность: имя и число игроков подтягиваются с самого сервера (по TCP
# на том же порту, что и игра). name — запасное имя, если сервер офлайн.
[[servers]]
name = "Локальный сервер"
address = "127.0.0.1:6000"
